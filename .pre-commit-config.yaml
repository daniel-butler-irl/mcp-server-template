repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
      - id: check-merge-conflict

  - repo: https://github.com/psf/black
    rev: 23.9.1
    hooks:
      - id: black
        language_version: python3

  - repo: https://github.com/pycqa/isort
    rev: 5.12.0
    hooks:
      - id: isort
        args: ["--profile", "black"]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.0.292
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.5.1
    hooks:
      - id: mypy
        additional_dependencies: [fastmcp]
        args: [--ignore-missing-imports]

  - repo: local
    hooks:
      - id: validate-manifest
        name: Validate DXT manifest
        entry: uv run python -c "import json; json.load(open('manifest.json'))"
        language: system
        files: manifest\.json$

      # pytest disabled due to mcp-cli dependency conflicts
      # Run manually with: uv run pytest tests/
      # - id: pytest
      #   name: pytest
      #   entry: uv run pytest tests/
      #   language: system
      #   pass_filenames: false
      #   always_run: true
